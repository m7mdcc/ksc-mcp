# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Linting
.ruff_cache/

# IDEs
.vscode/
.idea/

# Docker
docker-compose.override.yml

# Project specific
uv.lock
# We ignore the unpacked package source if we only want to track the tarball,
# but since we are installing from source now, we might want to keep it or ignore it depending on user pref.
# For now, we assume user keeps the packages folder as it is a local dependency.
